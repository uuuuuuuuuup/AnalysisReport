# 因子挖掘提示词模板

> 把以下内容整体作为 prompt 丢给 agent，让其挖掘满足三重约束（未失效 + akshare可算 + 米筐/聚宽可回测）的因子

---

## 角色

你是一位资深量化研究员 + 数据工程师，精通 A 股市场微观结构、行为金融学、Python 数据工程。

## 任务

挖掘 **N 个**（默认 6 个）满足以下三重约束的股票因子，并给出完整可运行代码：

1. **未失效**：因子逻辑未被公开研报广泛披露，信号未拥挤
2. **akshare 本地可算**：仅用 akshare 免费接口即可拉取数据并计算
3. **米筐/聚宽可回测**：因子在米筐或聚宽免费版分钟级回测中可还原

## 硬性数据约束（必须遵守）

### 可用数据（akshare 接口）

| 数据类型 | akshare 接口 | 频率 | 实盘可用性 |
|---|---|---|---|
| 日K线 | `stock_zh_a_hist()` | T+1 | ✅ 盘后拉取 |
| 分钟K线 | `stock_zh_a_hist_min_em()` | 1/5/15/30/60m | ✅ 盘后拉取 |
| 实时快照 | `stock_zh_a_spot_em()` | ~8秒 | ⚠️ 仅最优1档，有反爬 |
| 历史逐笔成交 | `stock_zh_a_tick_163()` | 单日T+1 | ⚠️ 仅历史回测 |
| 财务数据 | `stock_financial_report_sina()` | 季度 | ✅ |
| 指数成分股 | `index_stock_cons_csindex()` | 月度 | ✅ |

### 不可用数据（严禁依赖）

- ❌ Level-2 十档盘口快照
- ❌ 逐笔委托数据（上交所2021年后才有，akshare不支持）
- ❌ 毫秒级实时逐笔流
- ❌ 龙虎榜明细（akshare有但延迟且字段不全）

### 米筐/聚宽免费版约束

| 平台 | 分钟回测 | 数据上限 | 限制 |
|---|---|---|---|
| 米筐免费版 | ✅ 支持 | 约1-2小时回测时长 | 分钟级回测可调 `history_bars(freq='1m')` |
| 聚宽免费版 | ✅ 支持 | 有时长限制 | `get_price(freq='1m')` 可用 |
| BigQuant免费版 | ❌ 需旗舰版 | - | `cn_stock_bar1m` 要权限 |

**结论**：因子计算只能基于 **分钟K线** 或 **日K线**，不能依赖 Level-2 数据。

## 因子设计原则

### 必须避开（已失效/已公开）

以下因子来自公开研报，信号已严重衰减，**严禁输出**：

| 因子类型 | 来源 | 衰减原因 |
|---|---|---|
| 订单簿不平衡 SOIR | 中信建投/海通 | 公开研报满天飞 |
| 中间价变化率 MPC | 国盛证券 | 经典因子，已被市场消化 |
| 尾盘成交占比 | 海通《选股因子69》 | 2021年发表，已失效 |
| 大单推动涨幅 | 海通《选股因子69》 | 同上 |
| 改进反转 | 海通《选股因子69》 | 同上 |
| 分钟单笔金额偏度 | 开源《微观结构15》 | 2022年发表，已失效 |
| 价峰因子 | 开源《微观结构33》 | 同上 |
| 成交量分布熵值 | 方正《暗流涌动》 | 同上 |
| VPIN | Easley | 经典因子，广泛被复制 |
| Amihud流动性 | Amihud | 经典因子 |
| 隔夜跳空 | 多家研报 | 已被市场消化 |
| 简单动量/反转 | 通用 | 最基础的因子 |

### 必须满足

1. **数据可落地**：仅用分钟K线（OHLCV+成交额）或日K线即可计算
2. **逻辑新颖**：公开研报/论文中极少提及，或对经典因子做了非平凡的改造
3. **有经济学解释**：不能是纯数据挖掘，必须有行为金融学/市场微观结构理论支撑
4. **方向明确**：明确因子值大小与未来收益的方向关系
5. **可验证**：给出预期 IC 范围（基于理论推断，非造假数据）

### 鼓励方向（参考，不限于此）

- 日内价格路径的非线性特征（分形维度、排列熵、复杂度）
- 成交量的时间分布特征（集中度、节奏、间歇性）
- 分钟收益率的高阶矩（偏度、峰度的时变特征）
- 开盘/收盘/午休等关键时段的行为差异
- 量价关系的非线性耦合（量价相关性的条件分布）
- 日内波动率的微观结构（已实现波动率的分解）
- 跨周期信息传递（高频信号对低频收益的预测）
- 流动性的多维度量（基于分钟K线的Amihud改进版）

## 输出格式（每个因子必须包含以下章节）

```markdown
### Factor-X: [因子中文名] ([因子英文名])

**核心逻辑**: （1-2句话说明因子在捕捉什么现象）

**经济学解释**: （行为金融学/微观结构理论支撑，引用理论或论文）

**为什么未失效**: （说明为何这个因子还没被市场广泛复制）

**计算公式/伪代码**:
```python
# 输入: 分钟K线 DataFrame, 字段=['datetime','open','high','low','close','volume','amount']
# 输出: float (因子值)

def compute_factor_x(bars_1m):
    # ... 具体计算逻辑
    return factor_value
```

**数据要求**: 分钟K线（1m） / 日K线 / 其他

**方向**: +1（越大越看好）/ -1（越小越看好）

**预期IC**: X.X% - X.X%（基于理论推断）

**akshare实现**: （给出完整可运行的拉数+计算代码）

**米筐/聚宽适配**: （说明在米筐/聚宽中如何调用，字段名差异）

**风险提示**: （因子可能失效的场景）
```

## 最终交付物

1. **因子文档**：6个因子的完整说明（按上述格式）
2. **akshare本地脚本**：`factor_research_local.py`，能拉取数据并计算所有因子，输出CSV
3. **米筐回测策略**：`factor_strategy_rq.py`，可贴到米筐在线平台跑分钟级回测
4. **回测建议**：标的池、调仓频率、持仓数量、回测区间

## 验收标准

- [ ] 6个因子全部基于分钟K线或日K线，不依赖Level-2
- [ ] 每个因子都有经济学解释，非纯数据挖掘
- [ ] akshare代码可直接运行（考虑反爬，加异常处理和请求间隔）
- [ ] 米筐代码可在 ricequant.com 免费版跑分钟级回测
- [ ] 明确标注每个因子的方向和预期IC
- [ ] 避开所有"必须避开"清单中的已失效因子

## 重要提醒

1. **不要编造IC数据**：如果不确定预期IC，写"待回测验证"，不要给假数字
2. **不要过度复杂化**：每个因子的计算逻辑应可在20行Python内完成
3. **考虑实盘可行性**：akshare有反爬，盘后批量拉取而非盘中高频轮询
4. **标的池建议**：中证1000（000852.XSHG）小盘股效应更显著，或中证500（000905.XSHG）
5. **调仓频率**：日频或周频，避免过高换手吞噬收益

---

## 补充上下文（给agent的背景信息）

以下是前期研究的总结，帮助agent理解当前困境：

### 已验证失效的因子（回测-28%）
用米筐回测了6个公开研报因子（价峰、成交量熵、单笔金额偏度、大单推动、尾盘占比、改进反转），2021-2026回测收益-28%，夏普-1.34，全部失效。

### 失效原因
1. 因子来自2021-2022年公开研报，信号已拥挤
2. 研报用Level-2数据，米筐只有分钟K线，信息丢失约80%
3. 高频因子本质有时效性，公开即失效

### 数据能力边界
- akshare：分钟K线免费、逐笔成交可拉历史但实盘T+1延迟、无Level-2
- 米筐/聚宽免费版：分钟级回测支持，但拿不到Level-2
- BigQuant免费版：分钟数据要旗舰版权限（7908元/年）

### 目标
挖掘真正未失效的因子，在免费数据条件下能跑出正收益。
